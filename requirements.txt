# Core dependencies
pandas>=1.5.0
oracledb>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (DB_ADAPTER=postgresql; docker-compose full-stack)

# Database migrations
alembic>=1.13
sqlalchemy>=2.0
numpy>=1.23.0
python-dateutil>=2.8.0

# CLI
click>=8.0.0

# Reporting
Jinja2>=3.0.0

# Environment management
python-dotenv>=0.19.0

# Memory monitoring
psutil>=5.9.0

# Excel support
openpyxl>=3.1.0

# YAML configuration
pyyaml>=6.0

# JSON (NDJSON) parsing — JSONPath resolution for the JSON parser (ADR 0018,
# S19-1, #395). Pure-Python (no C extension); resolves per-field json_path
# selectors against each NDJSON record at parse time.
jsonpath-ng>=1.6.0

# XML parsing — hardened lxml parser for untrusted XML batch files (ADR 0019,
# S19-3, #396). Streaming iterparse + full XPath + a security-locked parser
# (resolve_entities/load_dtd/no_network disabled; DOCTYPE rejected) that
# defends XXE / billion-laughs / external-entity. Ships manylinux wheels (no
# system libxml build needed on RHEL); pin and track for CVEs.
lxml>=5.0.0

# API Framework (optional - for REST API)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
# Production process manager (S14-1, #412): the RPM valdo.service runs gunicorn
# with the uvicorn.workers.UvicornWorker class to serve src.api.main:app on :8000.
gunicorn>=21.2.0
python-multipart>=0.0.6
aiofiles>=23.2.1
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.28.0  # HTTP client for api_check test type and FastAPI TestClient

# Model Context Protocol server. Imported eagerly by `valdo serve`
# (src/main.py -> run_job_worker -> src/mcp/action_tools), so the runtime
# image cannot boot without it. Already pinned in requirements-api.txt;
# mirrored here so the Dockerfile image (which installs requirements.txt) runs.
mcp>=1.27.0  # requires Python 3.10+

# LDAPS login (when auth.enabled in config/ui.yml)
itsdangerous>=2.0   # Starlette SessionMiddleware cookie signing
ldap3>=2.9          # LDAPS bind + user search

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Data quality / Great Expectations
# NOTE: GE is not currently available for Python 3.14 in this environment.
# Use Python 3.11/3.12 runtime for GE execution.
great_expectations>=1.0.0; python_version < '3.14'

# Development tools
black>=22.0.0
flake8>=5.0.0
mypy>=0.990
pylint>=2.15.0

# PEX build tool (optional, for building PEX files)
pex>=2.1.0
